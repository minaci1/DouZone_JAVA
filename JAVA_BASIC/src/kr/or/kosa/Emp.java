package kr.or.kosa;

public class Emp {
	public int empno; // 속성, 멤버 필드(member field) >> 객체 변수(instance variable)
					  // 객체 변수는 초기화를 하지 않아도 된다.(어차피 객체마다 값이 다를 것이기 때문에..) >> default 값을 가진다 >> 0
					  // 생성되는 객체(사원)마다 다른 사번을 갖는다.
	
	public void infoPrint() { // 메소드, 함수
		int data; // 메소드, 함수 내에 있는 변수는 지역 변수(local variable)
	}
}
